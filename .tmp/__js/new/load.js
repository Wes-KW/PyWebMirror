// load

/* write this in each of the script to execute it in another context
    (function(){
        //
        // declare global variables
        // here or as the arguments
        //

        //
        //    script goes here
        //

    }).call(context);
*/

var server_url = "...";
var base_path = "..."
var base_url = server_url + base_path;
